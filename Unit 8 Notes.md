# Unit 8 – Similarity and Dilations

---

## 8.1 – Dilations

### Dilation
A transformation that **enlarges or reduces** a figure by a scale factor relative to a fixed point called the **center of dilation**.

### Scale Factor (k)
The ratio of a side length in the image to the corresponding side length in the preimage.
> k = image length / preimage length

| Scale Factor | Effect |
|---|---|
| k > 1 | Enlargement (image is bigger) |
| 0 < k < 1 | Reduction (image is smaller) |
| k = 1 | Congruent (same size) |

### Dilations in the Coordinate Plane
If center of dilation is the origin and scale factor is k:
> (x, y) → (kx, ky)

**Example:** Dilate △ABC with vertices A(2, 1), B(4, 1), C(4, −1) by scale factor k = 2:
- A′(4, 2), B′(8, 2), C′(8, −2)

---

## 8.2 – Similar Polygons

### Similar Polygons (~)
Two polygons are **similar** if:
1. All corresponding angles are **congruent**
2. All corresponding sides are **proportional**

### Scale Factor of Similar Polygons
The ratio of corresponding side lengths.

**Example:** △ABC ~ △DEF with AB/DE = BC/EF = AC/DF = scale factor

### Perimeters of Similar Polygons
If two polygons are similar with scale factor k, then the ratio of their perimeters equals k.
> Perimeter of image / Perimeter of preimage = k

---

## 8.3 – Similar Triangles: AA Similarity

### AA (Angle-Angle) Similarity Postulate
If **two angles** of one triangle are congruent to two angles of another triangle, then the triangles are **similar**.

> If ∠A ≅ ∠D and ∠B ≅ ∠E, then △ABC ~ △DEF

**Example:** Two triangles each with angles 106°, 31°, and 43° are similar by AA.

---

## 8.4 – Similar Triangles: SSS and SAS Similarity

### SSS (Side-Side-Side) Similarity Theorem
If the ratios of **all three pairs of corresponding sides** are equal, then the triangles are similar.
> AB/DE = BC/EF = AC/DF → △ABC ~ △DEF

### SAS (Side-Angle-Side) Similarity Theorem
If **two pairs of corresponding sides are proportional** and the **included angles are congruent**, then the triangles are similar.
> AB/DE = AC/DF and ∠A ≅ ∠D → △ABC ~ △DEF

---

## 8.5 – Proportionality Theorems

### Triangle Proportionality Theorem
If a line is parallel to one side of a triangle and intersects the other two sides, then it divides those sides **proportionally**.

> If DE ∥ BC in △ABC, then AD/DB = AE/EC

### Converse of the Triangle Proportionality Theorem
If a line divides two sides of a triangle proportionally, then it is parallel to the third side.

### Three Parallel Lines Theorem
If **three parallel lines** intersect two transversals, then they divide the transversals **proportionally**.
> If l₁ ∥ l₂ ∥ l₃ cutting transversals, then AB/BC = FE/ED

### Triangle Angle Bisector Theorem
If a ray bisects an angle of a triangle, then it divides the opposite side into segments that are proportional to the other two sides.

> If AD bisects ∠A in △ABC, then BD/DC = AB/AC

---

## Quick Reference – Similarity Criteria for Triangles

| Method | What to Show |
|---|---|
| **AA** | 2 pairs of congruent angles |
| **SSS~** | All 3 pairs of sides proportional |
| **SAS~** | 2 pairs of sides proportional + included angles ≅ |
